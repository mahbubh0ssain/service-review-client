import React from "react";
import { useTitle } from "../../Hooks/UseTitle/UseTitle";

const Blogs = () => {
  useTitle("Blogs");
  return <div>This is blog service component</div>;
};

export default Blogs;
